<div class="columns">
    <div class="column is-one-third"></div>
    <ul class="posts">
        {{#each posts as |post|}}
        <li>
            {{> post-info post }}
        </li>
        {{/each}}
    </ul>
</div>